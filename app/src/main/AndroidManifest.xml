<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cs.vault">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_vault"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_vault"
        android:supportsRtl="true"
        android:theme="@style/Theme.Test3">
        <activity
            android:name="com.cs.vault.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Test3.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>